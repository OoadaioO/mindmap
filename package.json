{
  "name": "mindmap",
  "version": "1.0.0",
  "description": "markdown to mindmap converter",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "markmap-lib": "^0.15.4"
  }
}
