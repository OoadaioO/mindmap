{
  "name": "mindmap",
  "version": "1.0.0",
  "description": "markdown to mindmap converter",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel deploy"
  },
  "dependencies": {
    "markmap-common": "^0.14.2",
    "markmap-lib": "^0.14.4"
  },
  "devDependencies": {
    "vercel": "^41.3.1"
  }
}
